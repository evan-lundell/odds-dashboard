services:
  mongo:
    image: mongo:7
    container_name: odds-dashboard-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: odds-dashboard

volumes:
  mongo-data:
